# Simple Server

A minimal Node.js HTTP server that responds with a hardcoded message.

## Usage

```bash
npm start
```

The server will start on `http://localhost:8000/`

To change the response message, edit the `message` variable in `server.js` and uncomment the desired network.

